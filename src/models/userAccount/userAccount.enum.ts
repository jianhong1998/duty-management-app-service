export enum UserAccountType {
    ADMIN = 'Admin',
    USER = 'User',
}

export enum UserAccountStatus {
    ACTIVE = 'Active',
    PENDING_APPROVAL = 'Pending Approval',
    DISABLED = 'Disabled',
}
